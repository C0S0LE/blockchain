import Cookies from 'js-cookie';

export default {
  /* install: (app) => {
    app.config.globalProperties.$cookies = {
      set: (name, value, options = {}) => Cookies.set(name, value, { path: '/', ...options }),
      get: (name) => Cookies.get(name),
      remove: (name) => Cookies.remove(name, { path: '/' }),
      has: (name) => !!Cookies.get(name),
    };
  }, */
};
